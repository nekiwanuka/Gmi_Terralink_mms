Django==5.0.6
djangorestframework==3.15.2
Pillow==10.4.0
python-dotenv==1.0.1
whitenoise==6.7.0
# PostgreSQL driver (production). SQLite is used in development with no driver needed.
psycopg2-binary==2.9.9
# Optional: MySQL driver (only if you choose MySQL instead of Postgres)
# mysqlclient==2.2.4
